<UserControl x:Class="Interpolation.MyControls.SliderWithTextBlocks"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Interpolation.MyControls"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <!--Левый текст-->
        <TextBlock Text="{Binding LeftText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Margin="4" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Right">
            
        </TextBlock>
        <!--Слайдер-->
        <Slider x:Name="MainSlider"
                Grid.Column ="1" 
                Margin="4" 
                VerticalAlignment="Center"
                IsSnapToTickEnabled="True"
                Minimum="{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Maximum="{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                TickFrequency="{Binding Step, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Value="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
            
        </Slider>
        <!--Правый текст-->
        <TextBlock Text="{Binding Value, ElementName=MainSlider}"
                   Grid.Column="2" 
                   Margin="4" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Right"> 
            
        </TextBlock>
    </Grid>
</UserControl>
